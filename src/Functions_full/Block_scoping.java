package Functions_full;

public class Block_scoping {
    public static void main() {
      /*  int a = 10;                 // see one varibale cannot be assigned to different datatype
        String a = "pavan";
        {
            int a = 20;             //we should to inialize new one to create new one by INT
            a = 20;                 // i can change the calue of the vairbale and then i can inicilize the same varible
            System.out.println(a);      // it will give the value inside the block
            String a = "pavan";        // same for block scoping we cannot assign the same variable
            int c = 10;               // I inicialized it here it will be correct but i cannot print outside
        }
        System.out.println(a);      // it will print value inside the block it will modify the orginal one
        System.out.println(c);      // see i cannot print outside the block scope the value of C

       */
    }
}
